*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/build/
build/

# Release signing keystore — SECRET, never commit
*.jks
*.keystore
keystore.properties

# Local release build outputs
/app/release/

# Firebase config — kept out of the public repo; add your own to build (see README)
app/google-services.json
